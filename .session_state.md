# Session state (Phase 4 debug)

## Root cause found and fixed
- Bug 1 (fixed earlier): `nexinfer/distributed/coordinator.py` `_dispatch` used `DeviceKind(d.get("kind", "/cpu"))` — "/cpu" is not a valid DeviceKind string-enum value (values are "/cpu", wait actual value IS "/cpu"... no, enum value is "/cpu" via DeviceKind.CPU = "/cpu". The default "/cpu" IS valid... but hello payloads with kind="cpu" failed. Fix applied: default to DeviceKind.CPU.value; also added try/except around _dispatch in _handle_control (never kill channel on bad msg).
- Bug 2 (the parity test deadlock): test main thread blocked asyncio loop during sync wait (time.sleep polling ready flag, os.system sleeps). Subproc connections queued in listen backlog, never accepted → no welcome → "worker subprocess never registered". FIX: drive loop from daemon thread (loop.run_forever), use asyncio.run_coroutine_threadsafe. Rewrote tests/test_distributed_parity.py completely (clean version).

## Changes made this session (NOT yet committed)
- nexinfer/distributed/coordinator.py: DeviceKind.CPU.value default; try/except around _dispatch; debug print "[COORD]" (REMOVE before commit)
- tests/test_distributed_parity.py: full rewrite (clean, no debug prints) — currently clean
- Earlier fixes (previous steps): kind=DeviceKind.CPU.value in hello payloads; stdin=subprocess.DEVNULL, stdout=ready.log, stderr=STDOUT in subproc Popen
- pyproject.toml still version 0.2.0 (needs 0.4.0); http_server.py version_string "nexinfer/0.3.0" (needs 0.4.0); README needs Phase 3/4 sections; zip to deliver

## TODO before delivery
1. Remove debug [COORD] print from coordinator.py
2. ruff check/format nexinfer tests
3. Run full suite: python3 -m pytest tests -q (expect ~97: 94 + 2 parity tests? only 1 parity test; minus any debug leftovers)
4. Kill leftover zombie worker procs before full run: pkill -f "_run_worker\|_worker_entry"
5. Update README (Phase 3+4), bump version 0.4.0, final lint+test
6. git add -A; commit "Phase 3+4 ..."; git push origin master
7. zip: cd /home/ubuntu && zip -r nexinfer-v4.zip nexinfer-work -x "*/\.git/*" -x "*__pycache__*" -x "*/egg-info*"
8. Deliver with summary

## Remaining notes
- test_distributed_parity.py marked @pytest.mark.network
- CI workflow f0ad9d1 cannot be pushed (token lacks workflows scope) — user must add via GitHub web UI

## Update (after context compaction)
- Parity test deadlock root cause (2nd bug) found: parent main thread blocked the asyncio loop during sync polling → subproc connections never accepted. Fixed by driving the loop from a daemon thread (loop.run_forever) + run_coroutine_threadsafe in test_distributed_parity.py (clean rewrite done).
- test_distributed_parity.py now clean, lint-clean (ruff passes, formatted). Test HUNG next: DistributedEngine.generate_stream ran rank0.generate on a NEW event loop (dedicated thread) → cross-loop asyncio.Queue traffic between rank0.generate's recv and the parent loop's server-side queue-feed tasks → deadlock.
- FIX IN PROGRESS: engine.py generate_stream now runs rank0.generate on the SAME loop as the rank-0 worker (worker.loop attribute). Worker.start() must set self.loop. STILL TODO:
  1. Add `self.loop = asyncio.get_running_loop()` in Worker.start() (nexinfer/distributed/worker.py line ~89)
  2. test_distributed_parity.py: root must expose .loop — Worker.start sets it; root started via run_coroutine_threadsafe → its loop is the test's loop-thread loop. OK.
  3. Re-run parity test.
  4. Note: abort_flag handling: engine abort check may now race on worker loop; abort_flag set by runtime.cancel() still works (root.generate checks abort_flag per step on worker loop).
- After parity test passes: ruff check/format ALL, full suite pytest, then README + version 0.4.0 + commit/push + zip + deliver.

## Progress update (after engine.py loop fix)
Root cause #3 found & about to fix: `_harness_http_server()` in test_distributed_parity.py deadlocks in `server.shutdown()` because `serve_forever()` is never started (BaseServer.shutdown waits forever). FIX: remove server.shutdown() (daemon threads + GC close fine) or start serve thread.

## State of files (Phase 4)
- tests/test_distributed_parity.py: has temporary TRACE prints (calling _replan, _replan done, plan ok, t1..t6, starting, distributed generate done) + extra "after assertion coord.plan" print — MUST REMOVE all TRACE prints before commit. Also has my engine+coordinator+worker fixes: coordinator [COORD] debug print ALREADY REMOVED (replaced with log.debug); coordinator _dispatch try/except + DeviceKind.CPU.value default; Worker.start() sets self.loop; engine.py uses run_coroutine_threadsafe on worker.loop (daemon thread + threading.Event, t join fixed, timeout=120.0).
- worker.py also has temporary [TRACE generate ...]/[TRACE run_decode_loop ...] prints — MUST REMOVE before commit.
- Parity test flow now works up to engine construction; w1 log shows waiting recv pp:0->1 (correct, no frame sent yet because hang was in harness).
- Remaining after fix: remove all TRACE prints, ruff check/format all, full test suite (expect ~97+), then README update (Phase 3+4 sections), pyproject.toml version 0.4.0, http_server.py version_string nexinfer/0.4.0, commit + push origin master, zip nexinfer-v4.zip (exclude .git, __pycache__, egg-info, .phase3_progress.md), deliver.
- CI workflow commit (f0ad9d1) can't be pushed (token lacks workflows scope) — user must add via GitHub web UI; mention in README.
- Key deliverable: /home/ubuntu/nexinfer-v4.zip
